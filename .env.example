# =============================================================================
# PERSONAL RESEARCH CONCIERGE AGENT - ENVIRONMENT CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Google Gemini API Key
# -----------------------------------------------------------------------------
# Get your free API key from: https://aistudio.google.com/app/apikey
# This is used for the AI agents (LLM calls)
GOOGLE_API_KEY=

# -----------------------------------------------------------------------------
# OPTIONAL: Google Custom Search Engine (CSE) Configuration
# -----------------------------------------------------------------------------
# For production-grade web search, set up Google Custom Search:
#
# 1. Create a Custom Search Engine at: https://programmablesearchengine.google.com/
# 2. Get your Search Engine ID (cx parameter)
# 3. Enable "Search the entire web" in your CSE settings
# 4. The same GOOGLE_API_KEY works for both Gemini and Custom Search
#
# NOTE: Without these, the system will use DuckDuckGo as fallback (free but less reliable)
#
GOOGLE_CSE_ID=

# -----------------------------------------------------------------------------
# OPTIONAL: Logging Configuration
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG shows detailed execution information
# INFO shows standard operation logs (recommended)

LOG_FILE=logs/research_concierge.log
# Path to log file (will be created automatically)

# -----------------------------------------------------------------------------
# OPTIONAL: Agent Configuration
# -----------------------------------------------------------------------------

# Maximum number of search results to gather per query
MAX_SEARCH_RESULTS=10

# Quality loop settings
MAX_QUALITY_ITERATIONS=3
# Number of times to improve the research brief

TARGET_QUALITY_SCORE=90
# Minimum quality score (0-100) to aim for

# -----------------------------------------------------------------------------
# OPTIONAL: Rate Limiting
# -----------------------------------------------------------------------------

# Requests per second for web scraping
WEB_SCRAPE_RATE_LIMIT=10

# Delay between API calls (seconds)
API_CALL_DELAY=0.1

# -----------------------------------------------------------------------------
# OPTIONAL: Advanced Settings
# -----------------------------------------------------------------------------

# Enable caching for search results (saves API calls)
ENABLE_SEARCH_CACHE=true

# Cache TTL in seconds (1 hour = 3600)
SEARCH_CACHE_TTL=3600

# Enable parallel processing for multiple documents
ENABLE_PARALLEL_PROCESSING=true

# Maximum number of parallel PDF processing tasks
MAX_PARALLEL_PDFS=3

# -----------------------------------------------------------------------------
# SETUP INSTRUCTIONS
# -----------------------------------------------------------------------------
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and add your GOOGLE_API_KEY
#
# 3. (Optional) Add your GOOGLE_CSE_ID for better search results
#
# 4. Run the system:
#    python main.py
#
# -----------------------------------------------------------------------------
# FREE TIER LIMITS (as of 2025)
# -----------------------------------------------------------------------------
#
# Google Gemini API:
#   - 60 requests per minute (free tier)
#   - 1500 requests per day (free tier)
#   - Rate limits are automatically handled by the system
#
# Google Custom Search API:
#   - 100 queries per day (free tier)
#   - 10,000 queries per day (paid tier: $5/1000 queries)
#   - Fallback to DuckDuckGo if limit exceeded
#
# -----------------------------------------------------------------------------
# TROUBLESHOOTING
# -----------------------------------------------------------------------------
#
# If you see "No GOOGLE_API_KEY found":
#   - Make sure this file is named .env (not .env.example)
#   - Make sure GOOGLE_API_KEY is set correctly
#   - Try restarting your terminal/IDE
#
# If search isn't working:
#   - System will use DuckDuckGo fallback automatically
#   - For better results, add GOOGLE_CSE_ID
#   - Check logs/research_concierge.log for details
#
# If you hit rate limits:
#   - Wait a few minutes before retry
#   - Reduce MAX_SEARCH_RESULTS
#   - System has built-in retry logic
#
# -----------------------------------------------------------------------------
